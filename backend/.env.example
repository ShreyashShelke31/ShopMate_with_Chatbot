# Example .env for backend
# Put your Gemini API key and (optionally) a Gemini-compatible endpoint here.
GEMINI_API_KEY=AIzaSyC4Tu46FaQw8wCBQ_8CVqlCImS5RNr5Mts
# Optionally set GEMINI_URL to a full HTTP endpoint that accepts POST JSON {"input":"..."} and returns JSON.
# For many Gemini setups you may need to adapt the code below to match the API request/response format.
GEMINI_URL=https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent
